<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs') %>
</head>

<body>
    <div class="container">
        <header>
            <nav>
                <a href="/lico/<%= name; %>"><%= name; %></a>
            </nav>
        </header>
        <section>
            <h1>All users</h1>
            <% users.forEach(user => { %>
                <p><a href="/lico/<%= user.userName; %>"><%= user.userName; %></a></p>
            <% }) %>
        </section>
    </div>

</body>

</html>